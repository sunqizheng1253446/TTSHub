ttshub/
├── cmd/                     # 程序入口
│   └── ttshub/              # 主服务入口
│       └── main.go          # 初始化模块、启动服务
├── internal/                # 内部私有模块（不对外暴露）
│   ├── tts_providers/       # TTS聚合模块（对接多平台）
│   │   ├── interfaces.go    # 定义TTS Provider统一接口
│   │   ├── manager.go       # Provider管理（负载均衡、选择策略）
│   │   ├── baidu/           # 百度TTS对接实现
│   │   ├── ali/             # 阿里云TTS对接实现
│   │   ├── openai/          # OpenAI TTS对接实现
│   │   └── ...              # 其他平台扩展
│   ├── api_server/          # OpenAI兼容API模块
│   │   ├── router.go        # API路由配置
│   │   ├── handler.go       # 处理TTS请求（调用provider+计费）
│   │   └── middleware.go    # API层中间件（认证、日志）
│   ├── billing/             # 计费模块
│   │   ├── models/          # 数据模型（余额、交易记录）
│   │   ├── repo/            # 数据库操作
│   │   └── service.go       # 计费业务逻辑（扣减、账单）
│   ├── user/                # 用户管理模块
│   │   ├── models/          # 用户、角色模型
│   │   ├── repo/            # 用户数据操作
│   │   └── service.go       # 注册、登录、权限控制
│   ├── coupon/              # 兑换码模块
│   │   ├── models/          # 兑换码模型（生成规则、状态）
│   │   ├── repo/            # 兑换码数据操作
│   │   └── service.go       # 生成、验证、充值逻辑
│   └── web_admin/           # 网页控制台后端模块
│       ├── router.go        # 后台API路由
│       ├── handler.go       # 后台管理接口（用户、计费、兑换码）
│       └── auth.go          # 后台认证逻辑
├── web/                     # 网页控制台前端资源
│   ├── public/              # 静态文件（CSS、JS、图片）
│   ├── src/                 # 前端源码（Vue/React等）
│   └── index.html           # 控制台入口页面
├── pkg/                     # 公共工具包（可复用）
│   ├── utils/               # 通用工具（加密、字符串处理）
│   ├── logger/              # 日志组件
│   ├── db/                  # 数据库连接池（GORM/PGX）
│   ├── config/              # 配置读取（YAML/Env）
│   └── middleware/          # 通用中间件（限流、CORS）
├── configs/                 # 配置文件
│   ├── app.yaml             # 全局配置（端口、环境）
│   ├── database.yaml        # 数据库配置
│   └── tts_providers.yaml   # TTS平台密钥/参数
├── scripts/                 # 辅助脚本
│   ├── migrate_db.sh        # 数据库迁移脚本
│   └── generate_coupon.go   # 批量生成兑换码工具
├── docs/                    # 项目文档
│   ├── api_docs.md          # OpenAI兼容API文档
│   ├── architecture.md      # 系统架构设计
│   └── deployment.md        # 部署指南
├── tests/                   # 测试目录
│   ├── unit/                # 单元测试
│   └── integration/         # 集成测试
├── web/                     # 前端控制台资源
│   ├── dist/                # 打包后的静态文件
│   └── src/                 # Vue/React源码（可选）
├── go.mod                   # Go模块依赖
├── go.sum                   # 依赖版本锁
└── README.md                # 项目说明